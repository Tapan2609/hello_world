#!/bin/bash
# create namespace for the family
# typically its family-<appnamespace> example :- family-forms
export app_family_namespace="[Namespace]"

cat > family-namespace.yaml<<EOF
---
apiVersion: v1
kind: Namespace
metadata:
  name: ${app_family_namespace}
---
EOF

kubectl apply -f family-namespace.yaml